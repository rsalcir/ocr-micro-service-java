# versao do docker instalada
docker -v

# construir imagem docker
docker build -f Dockerfile -t worker-ocr .

# listar imagens docker
docker images

# executar container
docker run -p 8085:8085 worker-ocr